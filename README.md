# Flask React App

##### Build

    cd backend
    python -m venv venv
    source venv/bin/activate

    pip install -r requirements.txt && cd ../frontend && npm install && npm run build && cd ..

##### Run

    cd backend
    flask run

